# Logistic-Regression
  Logistic regression is used to predict classification  problems(ie, binary outcomes for a given set of independent  variables. The dependent variables outcome is discrete (yes/no, true/false etc).  There are 2 categories,  1.Binary logistics regression(binary classification)  2.Multivariable logistic regression(multi class classification)
